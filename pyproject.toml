[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "akihabarai-score"
version = "0.17.2"
requires-python = ">=3.10"
dependencies = [
  "PyQt6>=6.6.1",
  "requests>=2.31.0"
]

[tool.setuptools]
packages = ["app"]
