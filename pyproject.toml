[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "akihabarai-score"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "PyQt6>=6.6.1",
]

[tool.setuptools]
packages = ["app"]
